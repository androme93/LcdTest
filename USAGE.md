# LCD测试程序使用指南

## 快速开始

### Windows系统

1. **安装Python环境**
   - 下载并安装Python 3.6或更高版本：https://www.python.org/downloads/
   - 安装时勾选"Add Python to PATH"选项

2. **安装依赖**
   ```cmd
   pip install -r requirements.txt
   ```

3. **运行程序**
   ```cmd
   python main.py
   ```

## 测试流程

### 第一步：设备检测

程序启动后会自动检测系统中的触摸设备：

- **检测到触摸设备**：直接进入测试流程
- **未检测到触摸设备**：提示用户选择继续或退出
  - 点击"继续测试"：可以继续测试（使用鼠标模拟触摸）
  - 点击"退出"：退出程序

### 第二步：亮点暗点测试

此测试用于检测LCD屏幕是否有亮点或暗点缺陷。

1. **测试流程**：
   - 屏幕会显示纯色背景（白色、黑色、红色、绿色、蓝色、青色、品红、黄色）
   - 每个颜色屏幕中央会显示一个随机的4位数字
   - 数字显示5秒后，会弹出输入界面

2. **操作说明**：
   - 记住屏幕上显示的4位数字
   - 在输入框中输入该数字
   - 点击"确认"按钮或按Enter键提交
   - 输入正确后进入下一个颜色测试
   - 如需跳过某个颜色测试，点击"跳过此测试"按钮

3. **注意事项**：
   - 仔细观察每个颜色背景下是否有异常的亮点或暗点
   - 亮点：在黑色或深色背景下出现的亮色像素点
   - 暗点：在白色或亮色背景下出现的暗色像素点

### 第三步：触摸测试

完成所有颜色测试后，自动进入触摸测试界面。

1. **界面说明**：
   - 屏幕显示12行×20列的触摸方块网格
   - 每个方块都可以被点击

2. **操作说明**：
   - 依次点击屏幕上的每个方块
   - 被点击的方块会变为绿色
   - 同时显示红色十字线，标识触摸的精确位置

3. **测试目的**：
   - 验证触摸屏的响应性：方块是否正确变色
   - 验证触摸位置的准确性：十字线中心是否在点击位置

4. **完成测试**：
   - 测试完成后，点击右下角的"完成测试"按钮
   - 或直接关闭窗口

## 常见问题

### Q1: 程序提示"未检测到触摸设备"怎么办？

**A**: 这可能是以下原因：
- 您的设备确实没有触摸屏
- 触摸驱动未正确安装
- 触摸设备被禁用

**解决方法**：
1. 检查设备管理器中是否有"人体学输入设备"或带"Touch"字样的设备
2. 确保触摸驱动已安装并启用
3. 如果确实没有触摸屏，可以选择"继续测试"使用鼠标进行测试

### Q2: 如何在全屏模式下运行？

**A**: 修改`main.py`文件中的以下代码：

```python
# 将这行
self.bright_dark_test.showMaximized()

# 改为
self.bright_dark_test.showFullScreen()
```

### Q3: 如何调整窗口大小？

**A**: 修改`main.py`文件中的`init_ui`方法：

```python
# 修改这行，调整为您的屏幕分辨率
self.setGeometry(100, 100, 1920, 1080)  # 1920x1080分辨率示例
```

### Q4: 如何跳过设备检测？

**A**: 如果您想跳过设备检测直接进入测试，可以修改`main.py`：

```python
# 在check_and_start方法中直接调用
def check_and_start(self):
    self.start_bright_dark_test()  # 直接开始测试
```

### Q5: 程序运行出错怎么办？

**A**: 请确保：
1. Python版本为3.6或更高
2. 已正确安装所有依赖：`pip install -r requirements.txt`
3. 在Windows系统上运行（Linux/Mac不支持设备检测功能）

## 开发和自定义

### 修改测试颜色

编辑`lcd_test_widgets.py`中的`BrightDarkSpotTest`类：

```python
self.test_colors = [
    ('white', Qt.white),
    ('black', Qt.black),
    # 添加或删除颜色
]
```

### 修改触摸方块数量

编辑`lcd_test_widgets.py`中的`TouchTestWidget`类：

```python
# 修改这些数字改变方块网格大小
for i in range(12):  # 行数
    for j in range(20):  # 列数
```

### 修改验证数字范围

编辑`lcd_test_widgets.py`中的`show_verification`方法：

```python
# 修改数字范围（当前是1000-9999，即4位数）
self.verification_number = random.randint(1000, 9999)
```

## 技术支持

如有问题，请参考项目README或联系原作者。

## 许可证

本项目遵循原项目的许可协议。
